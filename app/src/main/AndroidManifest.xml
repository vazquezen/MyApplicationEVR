<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".ui.captchascreen.GoogleReCaptchaActivity"
            android:exported="false"
            android:label="@string/title_activity_google_re_captcha"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".ui.captchascreen.FriendlyCaptchaActivity"
            android:exported="false"
            android:label="@string/title_activity_friendly_captcha"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".ui.success.SuccessActivity"
            android:exported="false"
            android:label="@string/title_activity_success"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>